# First project — 3 steps
1. Copy `projects/_TEMPLATE` → `projects/<PROJECT_ID>` and edit `state/PROJECT_STATE.json`.
2. Tell Assistant (Simple mode): what you want built (or answer ≤5 questions).
3. CoS reviews the handoff in OPUS ZERO Ops; on approval, grants Builder Mode for the specific target.
