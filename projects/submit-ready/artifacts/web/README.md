# SubmitReady web scaffold (shot C)

Vite + React shell. HashRouter stubs. Colophon footer hook. No pipeline. GitHub Pages base ./. Stack: React.

Dev: npm install && npm run dev
Build: npm run build
