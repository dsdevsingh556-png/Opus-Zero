# SubmitReady
PROJECT_ID: `submit-ready`
MODE: Operator / rigorous
SHAPE: Web app (browser-local portal submission packager)

Drop docs → presets (Job 2MB / Visa 5MB / Email 10MB / Custom) → local compress + format normalize + EXIF strip + safe rename → ZIP + 7-point checklist.

**Privacy:** files never leave the device. No upload · no account · no telemetry in v1.

**Host (v1 assumption):** GitHub Pages  
**PAAv3:** separate project — do not mix.

See `requirements/REQUIREMENTS_LOCKED.md` and `CREDIT.md`.
